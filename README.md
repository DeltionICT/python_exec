# Start python

### Functionaliteit
* Start python in terminal met Docker
* Docker moet geinstalleerd zijn !
* Het script download de python-image 
* Er wordt een volume gemaakt `app` dat correspondeert met de huidige werkmap.
* Start `.\run.ps1` vanuit powershell op windows vanuit de werkmap
* of Start `./run.sh` vanuit terminal op mac of linux vanuit de werkmap.